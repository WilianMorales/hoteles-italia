<nav class="fixed left-0 right-0 z-50 transition-colors duration-300 ease-in-out mt-2 m-2 xl:mx-16 rounded-2xl"
  role="navigation" aria-label="Main navigation"
  [ngClass]="{ 'bg-white shadow-lg': scrolled, 'bg-transparent': !scrolled }">

  <div class="flex items-center justify-between py-2 px-2">

    <!-- Logo -->
    <div class="w-14 flex-none">
      <img [src]="scrolled ? 'assets/img/Recurso3.svg' : 'assets/img/Recurso3-blanco.svg'" alt="Hotel Italia"
        class="h-auto w-auto transition-all duration-300" width="120" height="40" loading="lazy">
    </div>

    <!-- Links desktop siempre centrados -->
    <div class="hidden xl:w-64 xl:flex flex-1 justify-center gap-12">
      <a *ngFor="let link of links" class="text-lg min-w-[4rem] whitespace-nowrap transition-colors"
        [ngClass]="{'text-gray-700 hover:text-green-600': scrolled, 'text-white hover:text-white/70': !scrolled}"
        (click)="handleLinkClick(link)">
        {{ translate.t(link.key) }}
      </a>
    </div>


    <!-- Botones derecha -->
    <div class="flex items-center gap-4">

      <!-- Selector idioma -->
      <button (click)="changeLang(currentLang === 'es' ? 'en' : 'es')"
        class="relative w-16 h-7 flex items-center bg-gray-300 rounded-full p-1 transition-all duration-300">

        <!-- Círculo deslizante -->
        <div
          class="absolute left-1 flex items-center justify-center w-6 h-6 bg-white rounded-full shadow-md transition-transform duration-300"
          [ngClass]="{'translate-x-8': currentLang === 'es'}">
          <img [src]="currentLang === 'en' ? 'assets/img/pe.png' : 'assets/img/en.png'" alt="Bandera"
            class="w-5 h-5 rounded-full" width="20" height="20" loading="lazy">
        </div>

        <!-- Texto ES -->
        <span class="absolute left-10 text-xs font-bold text-gray-800 transition-opacity duration-300"
          [ngClass]="{'opacity-100': currentLang === 'en', 'opacity-0': currentLang === 'es'}">
          ES
        </span>

        <!-- Texto EN -->
        <span class="absolute right-10 text-xs font-bold text-gray-800 transition-opacity duration-300"
          [ngClass]="{'opacity-100': currentLang === 'es', 'opacity-0': currentLang === 'en'}">
          EN
        </span>

      </button>

      <!-- Reservar -->
      <button (click)="openModalForm()"
        [ngClass]="{'btn-reservar-scrolled': scrolled, 'btn-reservar-default': !scrolled }"
        class="px-4 py-2 rounded-xl transition-all duration-300 font-medium text-base">
        {{ translate.t('navbar.reservar') }}
      </button>


      <!-- Menu mobile -->
      <button (click)="toggleMenu()" class="xl:hidden focus:outline-none" aria-label="Abrir menú">
        <svg [ngClass]="{'text-white': !scrolled, 'text-gray-600': scrolled }" class="w-8 h-8" fill="none"
          stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" stroke-linecap="round" stroke-linejoin="round">
          <path *ngIf="!menuOpen" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>

  </div>
</nav>


<!-- Menu mobile -->
<div *ngIf="menuOpen"
  class="xl:hidden fixed inset-0 bg-black bg-opacity-95 text-white flex flex-col justify-between z-50 p-6" role="dialog"
  aria-modal="true">

  <button (click)="toggleMenu()" class="absolute top-6 right-6 focus:outline-none" aria-label="Cerrar menú">
    <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"
      stroke-linecap="round" stroke-linejoin="round">
      <path d="M6 18L18 6M6 6l12 12"></path>
    </svg>
  </button>

  <div class="flex justify-center mt-4">
    <img src="assets/img/hoteles-italia-logo-blanco.svg" alt="Logo Hoteles Italia" class="h-36" width="180" height="144"
      loading="lazy" />
  </div>


  <nav class="flex flex-col items-center space-y-6 mb-8" aria-label="Menú principal">
    <a *ngFor="let link of links" class="text-lg text-white hover:text-gray-400 transition-colors"
      (click)="handleLinkClick(link)">
      {{ translate.t(link.key) }}
    </a>
  </nav>

  <div class="text-sm text-white text-center">
    © 2025 Hoteles Italia — Todos los derechos reservados
  </div>
</div>
